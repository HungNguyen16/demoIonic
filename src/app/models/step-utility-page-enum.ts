export enum StepUtilityPageEnum {
    // Dang ky TTTB
    SimRegistration = 1,
    TakeCamera = 2,
    TakeCameraSim = 3,
    ConfirmInfo = 4,
    CompleteSimRegistration = 5,

    // SelectUtility = 0,
    // SimNumber = 1,
    // SimRegistration = 2,
    // ChangeLevelEsim = 3,
    // TransferSovereignty = 4,

    // Chuyen mang giu so
    NetworkSwitch = 30,
    ConfirmOtp = 31,
    ShowPopup = 32,
    // doi cap esim
    ChangeLevelEsim = 35,
    CompleteEsimRegistration = 36
}
